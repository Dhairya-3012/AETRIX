{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd ML && pip install -r ml_models/requirements.txt && python main.py --api",
    "restartPolicyType": "ON_FAILURE"
  }
}
