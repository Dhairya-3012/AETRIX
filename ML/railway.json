{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r ml_models/requirements.txt"
  },
  "deploy": {
    "startCommand": "python main.py --api",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
